{
  "name": "p3_reading_excel",
  "version": "1.0.0",
  "description": ":chart_with_downwards_trend: reading excel data with JavaScript :currency_exchange:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@types/xlsx": "^0.0.36",
    "ts-node-dev": "^2.0.0",
    "xlsx": "^0.18.5"
  }
}
